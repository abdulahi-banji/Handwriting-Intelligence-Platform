# Environment Variables for Render Deployment
# Copy these to your Render dashboard

# Required - Generate a secure random string
# You can generate one at: https://randomkeygen.com/
SECRET_KEY = your_random_secret_here

# Required - PostgreSQL connection string
# This will be provided by Render when you create the PostgreSQL database
DATABASE_URL = (Get this from Render dashboard → Your PostgreSQL → Internal Connection String)

# Optional - Google Gemini API key for AI features
# Get free key at: https://aistudio.google.com/app/apikey
GEMINI_API_KEY = (optional - leave empty if not using AI features)

# Optional - CORS origins (comma-separated)
# Use * to allow all origins, or specific domain like https://your-app.vercel.app
CORS_ORIGINS = *

# Note: The app will work with default values for SECRET_KEY and CORS_ORIGINS
# But DATABASE_URL must be set for the app to function properly

